/**
 * Utilities module
 */

const {
  formatWhatsAppNumber,
  formatPhoneForDisplay,
  extractCountryCode,
  isValidPhoneNumber
} = require('./phone');

module.exports = {
  formatWhatsAppNumber,
  formatPhoneForDisplay,
  extractCountryCode,
  isValidPhoneNumber
};
