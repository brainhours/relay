{
  "name": "relay-webchat-example",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency webchat example for @brainhours/relay-core (InMemoryStorage + SSE)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@brainhours/relay-core": "^1.9.0",
    "@brainhours/relay-webchat-widget": "^1.0.0",
    "express": "^4.18.2"
  }
}
